{{-- BAUER GROUP — Corporate LinkStack Theme
     Intentionally empty (enable_custom_body_end = 'false' in config.php).

     The page footer, its links (Impressum / Datenschutz / …) and the
     "Powered by LinkStack" credit are NOT part of the theme — they are
     managed in the LinkStack admin panel (Settings → Footer / DISPLAY_*),
     so they stay editable per deployment and are stored in the data volume.
     Keeping them out of the theme avoids duplicated, non-editable links. --}}
